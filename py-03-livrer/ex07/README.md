# releve

`releve` lit un fichier de mesures, ignore les lignes qui ne peuvent pas être lues en mode normal, et permet de compter ou de résumer les mesures par nom.

## Installation

Depuis la racine du projet :

```bash
pip install .
```
